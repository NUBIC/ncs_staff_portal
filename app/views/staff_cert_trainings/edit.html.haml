= render_breadcrumbs :separator => ' / ' 
= render 'staff_cert_trainings'

%h4 Edit certification/training for #{@staff.name}

= render 'staff_cert_training'

%p
  Note: Fields mark as (
  %em *
  ) are mandatory fields
