= render 'tabs'  
.content_body
  - if !@staff.external?
    .section_gap
    .section
      .section_heading
        New data collection task for #{@staff.display_name}
      = render 'data_collection_task'
  .section_gap
  = render 'data_collection_tasks'